# 採用UI・認証・同期・復元の検証記録

## 2026-09-13 GitHubアップロード

本人指定の `https://github.com/Takahiro0506/assessment-tracker.git` は送信前に参照なし（空）の状態を確認。originへ登録し、`codex/assessment-record` をpush成功。以下のURL確認待ちの記録は送信前の履歴。アプリのデプロイ、実D1へのmigration、公開範囲や課金設定の変更は行っていない。

## 2026-09-13 Git共有準備と待機日数修正

修正後は38件（単体18・統合10・クライアント10）、typecheck、lint、build成功。ステージしたテキストに本番設定値・代表的な秘密鍵／トークン形式が含まれないことを照合した。

結果待ち日数を画面の提出日と同じ端末日付基準に修正。Perthの午前0時前後、Sydneyの夏時間切替、未来日、単数形を自動テストに追加。`.env.example` のみGit除外の例外に追加し、本番設定や端末内データは引き続き除外。Gitへのアップロードは本人承認済みだが、送信先リモートが未設定のためURL確認待ち。

## 2026-09-13 読みやすさの調整

調整後の自動テスト37件、typecheck、lint、buildはすべて成功。

CSSのみ変更。提出記録10〜11pxを14pxに、カードの日付・操作を16pxに調整。狭い幅では列数を減らし、日付を折り返す。ローカルのサンプルを320px／390px／1440pxで目視し、各幅でdocument幅がviewport幅と一致することを確認。実機キーボードと初見利用者評価は今回の確認に含まない。

最終確認：2026-09-12。以下が現行 `app/` の結果。後半の2026-09-08〜10の記録は旧試作の履歴であり、旧「クラウド対象外」「1タブ前提」は現行仕様に適用しない。初見利用者・収益性の検証とは区別する。

## 2026-09-12 採用済みA-02／A-03の実装後

- `npm test` 17件、`npm run test:integration` 10件、`npm run test:client` 10件の計37件が成功。`npm run typecheck`、`npm run lint`、`npm run build`も成功。
- 新規自動試験でISO日付に加えて `16 October 2026`、`Friday, 16 Oct 2026`を受理し、存在しない英語日付を拒否することを確認。既存の利用者分離、CAS競合、操作ID再送、通信失敗時の端末コピー、復元の試験はすべて維持。
- ローカル認証エミュレーター＋D1の架空アカウントで、空状態→名前だけ追加→+ Unit→+ Due date（英語直接入力と曜日表示）→提出→結果待ち→再提出→以前の締切・提出日時を履歴確認→再提出後の完了を確認。
- 1440pxでNext up、Overdue（超過日数）、Today、Coming up、No date yet、科目集計と月別／科目別切替を確認。390pxでSemester / Submissions下部2タブ、固定入力、24件の縦一覧末尾が固定UIに隠れないことを確認。ソフトウェアキーボードそのものと実機は未確認。
- 開発サーバーを一時停止した実ブラウザで、`Not saved yet`、端末コピーがある旨、Details、操作抑止を確認。再起動後に未同期の25件目が自動再送され、警告が消え操作可能に戻ることを確認。
- Git除外の本番設定、Wrangler認証、実D1の読取接続を再確認。アプリ用テーブルは0件のまま。migration、デプロイ、外部URL発行、有料契約は実施していない。
- これは開発者によるブラウザ試験であり、説明なしの初見利用者評価、需要・支払意思、収益性の検証ではない。

## 2026-09-12 CLI認証後の読取確認

本人のWrangler許可後、whoamiでOAuthログインを確認。D1 infoで指定DB ID／名前一致・アプリ用テーブル0件を確認。sqlite_schemaのSELECTではCloudflare内部テーブル_cf_KVのみ。changes 0／rows_written 0で、DBを書き換えていない。課金プランの独立確認、migration、Workerアップロード、外部URL有効化は未実施。限定テストのアクセス範囲を本人に確認する段階。

## 2026-09-12 実設定の接続準備

本人提供のFirebase公開設定と実D1 IDをGit除外のwrangler.production.jsonに反映。エミュレーターフラグなし、公開・プレビューURL無効、分割JSのESModule rulesを確認。アプリ・DBスキーマの変更なし。ビルドとWranglerのdry-run成功（追加49モジュール、静的資産20ファイル、gzip約315 KiB）。既存36件のテストは直前の初回体験変更時に成功しており、この設定のみの変更では再実行していない。

whoamiは未ログイン。認証・リモートDBの内容／プラン・実トークンは未確認。migration・アップロード・URL発行なし。dry-runのbindings一覧は構成の読込確認であり、実D1接続成功ではない。本人へのログイン手順は[接続手順](cloud-setup.md)に記載。

## 2026-09-12 初回体験の改善後

- 自動チェックは36件成功（従来30件を維持＋6件追加）。npm test 16件、test:integration 10件、test:client 10件。typecheck・lint・build成功。
- 追加試験：科目・日付なしでの追加／JSON往復／元データ不変、科目後付け時の旧スナップショット保持、次の課題の締切順・提出済み／ごみ箱の除外、日数の年越し／閏日、入力送信の受理時だけクリア、失敗時の入力保持、保存待ち中の二重送信抑止。
- ローカルの架空の新規アカウントで、空一覧→見本を1クリックで表示→戻って空一覧→課題名＋Enterで1件表示→再読み込み後の保持を確認。
- サンプル内で課題追加・新しい科目名を後付け・今日の日付を入力。日付の読み返し、Due today、超過日数を確認。サンプルから戻ると元アカウントの3件のままで、見本内の追加カードは含まれない。
- 390px・1440pxの空一覧を目視。320pxで一覧の横幅が320のまま、SettingsからBackup & recoveryが利用可能。移動後も保存版の説明・履歴・元データ非上書きの記載が残っている。
- API、版トークン、同期フック、SQLスキーマ、旧データ移行処理は今回変更していない。ファイル単位の保全比較でも確認。
- 新しい保存エラー表示は概要とDetailsを分離。通信失敗・端末コピー保全の注入は従来のJSDOM試験で確認。今回、実ブラウザの通信断を注入する追加試験は行っていない。
- 初見利用者が「2秒で分かる」「認証を含めて1操作」は未検証。[初見評価用紙](first-use-evaluation.md)で別途測る。アカウント不要モードは初見評価後に判断。
- 改善直前のソース130ファイルは `/Users/takahiro/Developer/assessment-tracker-recovery/before-first-step-20260912-152537/` にsource.tar.gzとSHA-256 manifestを保管。

## B案実装時点の自動チェック（以下は改善前の記録）


- `npm test`：13件。締切、再提出の履歴、JSON検証、旧保存層の保護。
- `npm run test:integration`：10件。独立したMiniflare/D1で同一アカウントの共有、他人の現在データ／履歴へのアクセス拒否、同時更新の条件付き保存、応答消失・同時再送の重複防止、再提出・ごみ箱・保存版復旧、旧データの繰り返し移行、認証拒否、容量・DB失敗、別DBへのSQL復元、復元後の版トークン衝突を確認。
- `npm run test:client`：7件。JSDOMで送信前の未同期コピー保存、確認応答までSaving、容量不足時の送信拒否、オフライン中の保全、リロード後の再送、応答消失後の新しいサーバーデータ保持、競合の双方保管、別アカウントへの復旧コピー非表示。
- `npm run typecheck`、`npm run lint`、`npm run build`：成功。ビルドのルート分類UnknownはVinextの静的解析に関する情報表示で、ビルド失敗ではない。
- npm監査：本番依存0件。開発依存を含む全体は14件（moderate 10／high 4）。開発ツールの推移依存が残るため、公開前の再監査・互換性を確認した更新が必要。強制更新は行っていない。

## ローカルブラウザで確認

Codex内ブラウザ、localhost:3001、Firebase Auth EmulatorとローカルD1、架空アカウント／課題のみ。

- 登録・ログイン、科目を一度選んで課題2件をまとめて保存。初期名と任意の締切。同じアカウントの別タブで同じ内容を表示。
- カレンダー選択と直接入力。不正な2026-02-30は拒否。正しい日付を保存。
- 提出→結果待ち→再提出。旧締切9月18日と提出日時を保持し、新締切9月25日・round 2を表示。
- ごみ箱への移動・復元、完了・完了取り消し、別科目の下書きを離脱後に再開して保存。
- PC幅1440pxとスマホ幅390pxでカード・入力画面を目視確認。最終320pxではヘッダーを調整し、一覧と日付ダイアログの横はみ出しなし（document幅320、ダイアログ幅296／左12）。
- `Saved · local test` とローカルテストの説明を表示し、実クラウド保存と誤認させない。

別タブ確認は実PC／スマホ間の試験の代替ではない。通信切断や容量不足は上記のJSDOM・API試験で注入し、実機ブラウザ障害の再現とは区別する。

## 独立したバックアップの復元

`node scripts/backup-d1.mjs --local wrangler.local.json .local-backups` でSQLとSHA-256 manifestを作成。別IDのローカルD1へWranglerでSQLを読み込み、再エクスポートして比較。workspaces 1行、revisions 3行、履歴トリガー2件が一致した。自動統合試験でも独立DBへの復元を確認した。

アプリのJSON／保存版の復元は追加方式・同一内容スキップ・衝突時の別コピーを試験した。旧データ原本を変更せず、科目の大文字小文字や空白が違うケースも、以前の締切・提出履歴を保持して再取込の重複を防ぐ。現行UIからのファイル選択・復元の全組合せは未実施。

ローカルSQLはクラウドTime Travelの実行実績ではない。日次バックアップの自動化と別保管先は未設定。Firebaseのユーザー情報はD1 SQLには含まれない。[復元手順](recovery-runbook.md)を参照。

## 残る確認

- 本人のFirebase／Cloudflareアカウント設定後の実メール、Googleログイン、実トークン署名検証、実PC／スマホ同期。
- 実D1 Time Travel、運用バックアップ、WorkerのCPU・リクエスト数・DB容量が無料枠に収まるかの測定。
- iOS Safari／Android Chrome実機、スクリーンリーダー、200%文字拡大、長期容量・大量履歴。
- 初見学生による使いやすさ、入力時間、需要・支払意思。

公開・有料契約は実行していない。[外部設定手順](cloud-setup.md)を参照。

---

# 旧試作の検証履歴（現行仕様ではない）

# 操作試作の検証記録

初回実施：2026-09-08。UXレビュー後の追加確認：2026-09-10。開発者による動作確認。初見利用者の評価、支払意思、収益性の検証とは区別する。

## 自動チェック

- `npm test`：13テスト成功（初回6件＋追加7件）。名前の整形／空白拒否、締切未定、複数回の再提出と訂正による履歴保持、JSON往復、壊れたJSON／バージョン違い／重複ID／不正な日付／最新履歴との不一致の拒否、締切順、平常時・集中時データの整合性と履歴順を確認。
- `npm run typecheck`：成功。
- `npm run lint`：成功。生成済みの未編集UI部品 `components/ui/**` と `hooks/use-mobile.ts` はlint対象外。アプリ本体・データ処理・テスト・設定を検査する。
- `npm run build`：成功。Vinextによるルートの静的分類はUnknownという情報表示が残る。ローカルの `/` はHTTP 200で描画できる。
- 依存関係を修正済みバージョンに更新し、npm監査は既知の脆弱性0件（この時点）。公開時は改めて確認する。

## 2026-09-10の追加検証

- 期限の分類境界：今日、6日後、7日後、年末年始、閏日を自動テスト。最近の科目の重複除去・件数上限と完了取り消し時の履歴保持も検証。
- 保存層を分離し、偽ストレージを使って他タブ競合、破損、利用拒否、容量不足、不正な置き換えによる既存データ保護を自動テスト。ブラウザ障害そのものの注入とは区別する。
- サンプル内で追加・完了・取り消し・再提出を実行。終了前後の自分のJSONバックアップを比較し、全課題・履歴が一致することを確認（ExportのsavedAtは比較対象外）。
- 件数ボタンで対応する見出しへフォーカス移動、科目チップから選択してEnterで登録、完了の取り消し、再提出入力への直接移動を確認。
- 完成ビルドを別ポート3002で一時起動し、空一覧のTry a sample week、Start my list、再読み込みによる練習終了を確認。通常の保存領域3001は上書きしていない。
- デスクトップとスマホ390pxで、サンプル表示・状態操作・画面下のUndo completionを目視確認。320pxで入力欄へのフォーカスと横方向のはみ出しがないことも確認。
- 原案の収益化に関する断定は出典つきレビューで訂正。UX効果・初見利用者評価・購入意思は未検証。

## 初回にブラウザで確認したこと（2026-09-08の挙動）

Codex内ブラウザ、ローカルURL `http://localhost:3001/`、架空データのみを使用。

- Enterによる連続登録、前の科目の引き継ぎ、追加後の課題名へのフォーカス。
- 期限未定の登録と一覧先頭の独立表示。
- 提出済み→結果待ち→期限未定の再提出→作業対象への復帰。
- リロード後も状態・履歴が残り、過去の提出日時と以前の期限がHistoryで確認できる。
- 一覧内で名称・日付・状態を訂正。空白だけの名称は拒否し、元データは保持。
- Update statusから完了へ移動し、履歴が残ること。
- 明示的な削除確認と削除結果。完了操作では削除されない。
- Exportで生成された実ファイルを検証し、その同じファイルをRestoreして成功。読込後の置き換え確認・件数表示。
- 不正なバックアップの拒否と、集中時の既存データ24件の保持。
- 平常時7件／集中時24件の読込。
- PC幅1280pxとスマホ幅390pxの見た目、320pxで横方向のはみ出しがないこと。集中時も390pxで横スクロールなし。
- スマホのAddから入力欄へ移動。編集開始時に科目欄へフォーカス。ダイアログの初期フォーカス。

初回のプレビューには平常時の架空データを置いた。2026-09-10以降のサンプル機能は自分の一覧と分離される。以前の保存データをサンプルと推測して自動削除する処理は入れていない。新しい保存領域では空一覧から開始する。

## 実装済みだが追加確認が必要なケース

保存容量不足・ストレージ利用拒否は例外を捕捉し、更新を確定しない。保存済みデータの破損は自動上書きを止める。別タブの変更はstorageイベントと書込直前の比較で検知する。これらのブラウザ障害そのものの注入テストは未実施。2026-09-10に、分離した保存層で失敗・競合を再現する自動テストを追加済み。localStorageの完全同時書込を排他制御するものではないため、1タブでの利用を前提とする。

実機のiOS Safari／Android Chrome、スクリーンリーダー、200%文字拡大、長期運用と大量履歴の容量上限は未評価。サーバー同期・認証・決済・外部サービス接続・公開環境は今回の対象外。

## 初見利用者の評価（次の作業）

平常時と課題集中時を分け、同じ操作順で確認する。本人が苦労した週を再現する場合も、この開発リポジトリには実在の学生情報を入れない。

1. 科目と課題を続けて登録し、一部の締切を未定にする。
2. 未提出・結果待ち・期限未定の課題を見つけてもらう。
3. 提出、再提出、期限訂正、完了を行い、前回の提出記録を探してもらう。
4. バックアップを書き出し、復元してもらう。

登録時間、登録ミス、状態把握の正確さ、迷った場所を記録する。開発者の説明が必要だった箇所も残す。数秒の更新時間差や少人数の感想だけから需要・価格・購入意思を断定しない。
